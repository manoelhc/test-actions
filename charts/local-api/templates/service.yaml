apiVersion: v1
kind: Service
metadata:
  name: {{ include "local-api.fullname" . }}
  labels:
    {{- include "local-api.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.containerPort }}
      protocol: TCP
      name: http
  selector:
    {{- include "local-api.selectorLabels" . | nindent 4 }}
